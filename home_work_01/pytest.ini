[pytest]
# Run from home_work_01/; put the unit directory on sys.path so `import ingestion`
# and `from tests.conftest import ...` both resolve. The suite is fully offline.
pythonpath = .
testpaths = tests
