# Audit record — Issue #41，cycle 1，R1（Formal Ticket independent audit）

| 欄位 | 內容 |
| --- | --- |
| Work Contract | GitHub Issue **#41**（`yotsubamomo/aiot-classwork`）「V2 整合驗收：README 與 CONTEXT 最終審查、V2 驗收文件、定向 V1 重驗、CI 全綠與部署 preview 驗證」（`gh issue view 41`：OPEN，label `ready-for-agent`，blocked-by #37、#40——Reviewer 以 `gh issue view` 確認 #35～#40 全部 CLOSED）。性質 **INTEGRATION／FINAL VERIFICATION**（非 scope class，不承載新需求）。上位：V2 Outcome Contract `home_work_01/doc/governance/outcome-contract-v2.md`（ACCEPTED 2026-09-25，normative candidate `69c5a04`）；Delta Spec `home_work_01/doc/spec/SPEC-V2.md` **v2.2**（DV-23 註記更正 `2771a54`；§2.8 R-V2-DOC-1～6、§2.9 R-V2-TC-3／TC-4／ENV-1／ENV-2、§3 AC-V2-03（preview）／16／17／20／21／22、§6.3、§9 前置條件）；derivation record `derivation-SPEC-V2.md`（§6 A-1／A-5／A-6、§11 #1／#4、DV-16、§15）；`decisions/decision-20260923-high-risk-categories.md`（A-1）。本票範圍：AC-V2-21（十四項）、AC-V2-20（CI、blob／diff、§6.3 彙整）、AC-V2-16（最終 network log）、AC-V2-17(e)（與 (a)(d) 的最終核對）、AC-V2-22 非金鑰部分、R-V2-DOC-3、Bindings §5 release-gate 材料；**AC-V2-17(c)、AC-V2-22 觀測部分、AC-V2-03 preview 抽樣、A-6 preview 紀錄 BLOCKED（RB-3）**。 |
| 受審 subject | branch `home_work_01-v2-implementation`；BASE **`df78e79`** .. HEAD **`7588583`**；**final subject `9902026cc41725524f154185aeebd59057fbdadf`**（README 與瀏覽器檢查去 flake 在 `49dac12`；`49dac12..9902026` 只改 `doc/acceptance/`（Reviewer `git diff --name-only` 確認）；`9902026..7588583` 只改 `doc/governance/worklog/issue-41.md`；審查時本機 HEAD `38e20fa`（`7588583..38e20fa` 只改 run record）——皆 record-only（Bindings §7））。`git diff --name-only 08e158e 9902026` 單元目錄外 0 檔（RB-5）。V2 全段 `08e158e..9902026` 亦審查其與 V1 結案版 `ef15d3e` 的 blob／diff。 |
| Audit 種類 | **R1**（Formal 必做的 Ticket independent audit；治理 §4.1、§4.4），**cycle 1**。**不是** Spec Integration Audit（§4.7 另派）；本紀錄只附 SIA 參考觀察（§7）。 |
| 角色 | `primary_reviewer`（definition `gov-primary-reviewer`；Bindings §3.1 mapping `claude-opus-5-5`／`xhigh`） |
| Binding 證據 | 正式核對由派工者（Orchestrator）依 Bindings §3.4 記入 run record `home_work_01/doc/governance/run/run-20260925-hw01-v2-formal.md`（2026-09-26 #41 Executor return 列：`a7f0755757b237151` = `gov-executor`、`claude-opus-5-5`／`high`）。Reviewer 另以 Bindings §3.4 指令唯讀 harness 紀錄（session `05b8408b-260a-46d3-a4fe-ec07e3ec365a`）自行觀察：`agent-a16d3b7e242f9d1aa` `gov-primary-reviewer` `[('claude-opus-5-5', 'xhigh')]`（本 Reviewer；第一則訊息即本次 #41 R1 派工）；`agent-a7f0755757b237151` `gov-executor` `[('claude-opus-5-5', 'high')]`。與 Bindings §3.1 一致。此觀察只供對照，不取代派工者的核對。 |
| Independence（治理 §2.3、§4.6） | (1) **Fresh context**：未繼承 Executor 對話；worklog `issue-41.md`、`ACCEPTANCE-V2.md`、commit message、已提交的 `screenshots/v2/issue-41/` 證據與派工文字中轉述的 Executor concerns 一律視為待驗證主張。(2) **Binding**：見上一列。(3) **Autonomous access**：自讀 Issue、SPEC-V2 v2.2、derivation record、decision A-1、全部 #35～#40 audit records（verdict 行）、run record、README 全文（1191 行）、`CONTEXT.md`、BRIEF-V2 §9、git 歷史與 diff、CI run 與 log、GitHub deployment API；以 `git archive` 匯出 V1 `ef15d3e` 與 final subject `9902026` 到 Reviewer scratchpad；自寫 `rev_fwd.py`（預報 `/api/` 封網無金鑰比對）、`rev_keyscan.py`（金鑰格式掃描，不讀 `.env`）、代表測站手算、anchor slug 檢查與一次 mutation test；重跑全套 pytest、`check_radar_browser.py`、`check_modes_browser.py`（×3）與 `check_series_error_visible.py`（輸出導向 scratchpad）；對 final subject 的 preview 以一般 GET 與 `smoke.py` 實測。(4) **Self-written record**：本紀錄由 Reviewer 以自己的 Write 工具寫入。**Model diversity：`diversity_lost`**——Executor 與 Primary Reviewer 同為 `claude-opus-5-5`（Bindings §5、b2 override；decision A-7）。合法狀態，不減損 §2.3 的 independence。 |
| 日期 | 2026-09-26 |

## 1. 審查方法與環境

- **環境**：Windows 11；`home_work_01/.venv` Python 3.12.14；Chrome headless（DevTools protocol，`websocket-client`）。
- **憑證（RB-3、A-3）**：Reviewer **沒有讀取** `home_work_01/.env`，**沒有使用任何 CWA 金鑰**，**沒有進入 Vercel、沒有讀取或填入 Vercel 環境變數**。全部本機測試以 `env -u CWA_API_KEY` 在匯出樹（無 `.env`）執行；瀏覽器檢查一律用其內建的模擬上游與哨兵／placeholder 金鑰。金鑰外洩檢查只用金鑰格式比對（`ingestion/checks.py` 的 `KEY_PATTERN` 與一個更寬的大小寫不敏感版本）。
- **對外請求**：只有 (a) `gh` 讀取 Issue、CI run／log、GitHub deployments；(b) 對 final subject 的 **public preview** 發出一般 GET（`/`、`/api/health`、`/api/observations/latest`、`/api/radar/latest`、`/static/app.js`）與 `smoke.py`。沒有發出任何 CWA 請求。
- **不寫入**：沒有 git 寫入、沒有修改追蹤中的檔案（本紀錄除外）；結束時 `git status --short` 只有審查開始前即存在、與本票無關的 `grep.exe.stackdump`。
- **Scratchpad 揭露**：scratchpad 由本 session 各角色共用。Reviewer 在第一次匯出前執行 `rm -rf scratchpad/v1 scratchpad/fin` 後重建，**事先未列出這兩個名稱是否已存在**；若其他角色曾使用同名目錄，其未追蹤的暫存內容已被取代（任何 worklog／audit 紀錄都未引用 `scratchpad/v1`、`scratchpad/fin`——Reviewer `grep` 確認）。另新增 `data_9902026.db`、`fwd_v1.json`、`fwd_fin.json`、`rev_*.py`、`rev_*`／`mut` 目錄。只影響未追蹤的 scratch 檔，不影響任何已提交證據。
- **Commit 衛生**：`49dac12`、`9902026`、`7588583` message 皆為 `[Modify] – …`＋`[Additions]／[Modification]／[Fix] – …` 格式；無 `Co-Authored-By`／Claude／`Generated with`。`main` 仍為 `08e158e`（未合併，RB-1 保持）。

## 2. 分配項目的逐條結論

| 項目 | 判定 | 證據（Reviewer 自行取得） |
| --- | --- | --- |
| **AC-V2-21** 文件審查十四項 | **PASS**（逐項見 §2.1） | README `9902026`（1191 行）逐行閱讀；README 內 52 個 `](#…)` 錨點以 GitHub slug 規則自寫檢查 **全部可解析**（25 個不同目標）。 |
| **AC-V2-20** CI、V1 測試保留未弱化、blob／diff、預報 `/api/` 回應、§6.3 | **PASS** | **CI**：run **`36232697465`**，headSha `9902026cc4…`，conclusion success；log：`Python 3.12.14`、`614 passed in 9.63s`、`credential scan passed: 804 tracked files; no .env tracked (only .env.example); no CWA-key-format string in tracked files or committed history; no Authorization value in fixture/raw JSON.`（其後 `7588583`、`38e20fa` 亦 success）。**本機**：匯出 `9902026`、無金鑰無 `.env` → `pytest -q` **614 passed**。**Test id**：自收集 V1 `ef15d3e` **259** 個、`9902026` **614** 個；V1 缺失 **0**。**V1 test／tool 檔 diff（`ef15d3e..9902026`）**：只有 `test_secrets.py`、`test_static_checks.py`、`tools/credential_scan.py`（+256／−9）；刪除行只有 `_PYTHON_SIDE`／`_NON_SHARED_PYTHON` 兩個定義行（分別以 import-closure 版與延伸版取代，R-V2-SEC-4 (a′)(c)、DV-15——已由 a35-r1 審查）與 docstring 文字，**無任何 assert 被刪除或放寬**；其餘為新增（V2 樣本、V2 程式清單、closure 測試）。**Blob／tree**（`git rev-parse`，`ef15d3e`＝`08e158e`＝`df78e79`＝`49dac12`＝`9902026`＝`7588583`＝HEAD）：`app.py` `5693be811edc`、`weather_query.py` `4d2e92f4f385`、`data.db` `687586991ce3`、`ingestion/` `91df24e32fa7`、`data/raw/` `00cb0b88e875`、`requirements.txt` `f5199f4850d7`、`smoke.py` `5f09950369d7`、`vercel.json` `83dc6a14c249`、`.python-version` `e4fba2183587`、`.github/workflows` `edd4a9be3cd1`；`doc/requirement/` 對 `ef15d3e` 無 diff；V1 凍結範圍內唯一 diff 是 `api/index.py` docstring（#35，3+/1−，行為不變）。**預報 `/api/`（Reviewer 自寫 `rev_fwd.py`）**：`ef15d3e` 與 `9902026` 各在子行程中封鎖 `socket.connect`／`connect_ex`／`create_connection`／`getaddrinfo`（並計數嘗試）、移除 `CWA_API_KEY`，以 Flask test client 對 **6 種 DB 狀態**（committed、missing、0-byte、非 SQLite 檔、空表、少一列）× **21 個路徑**（health、regions、days、六個 series（百分比編碼）、兩個未知 Region、七個 day、未列日期、`not-a-date`、`/api/unknown`）比較 status＋Content-Type＋body sha256：**126 組、0 差異、兩側網路嘗試 0 次**（status 200×16、404×10、503×80、500×20——500 只出現在「非 SQLite 檔」狀態，V1 與 V2 同為 500，屬 V1 既有行為、不在 V2 範圍）。**§6.3**：見本表 §6.3 列。 |
| **AC-V2-16** 最終執行期 network log（Now mode 含 Radar 顯示、Forecast mode）零外部請求 | **PASS** | **Reviewer 自行重錄**：在匯出的 `9902026` 樹執行 `tests/check_radar_browser.py --out <scratchpad>` → **47/47**；自行解析其 `network-log.json`（CDP `Network.requestWillBeSent`，`Network.enable` 先於導航）：5 個情境共 **113** 個請求，**外部 0**（每個都是 `http://127.0.0.1:<port>/…` 或同源 `blob:`）；`1280-main` 23 個：`/`、5 個 `/static/**`、`/favicon.ico`、`/api/health`、`/api/regions`、`/api/regions/北部地區/series`、`/api/days`、**`/api/days/2026-09-24`（Forecast mode）**、`/api/observations/latest` ×2、**`/api/radar/latest` ×4**、同源 `blob:` ×4。另讀已提交的 ev41 五個 log（modes 50、refresh 203、county 135、fence 505、radar 113，合計 1,006）：外部 **0**（數字與文件不符，見 F-1）。另 `check_modes_browser.py` 三次重跑皆記錄零外部。 |
| **AC-V2-17(a)** `.env` 未追蹤；掃描零命中；樣本無 `Authorization` 值 | **PASS** | `git ls-files` 只有 `home_work_01/.env.example`；`git check-ignore -v home_work_01/.env` → `home_work_01/.gitignore:12`（root `.gitignore:11` 亦忽略）。CI 憑證掃描通過（見 AC-V2-20）。Reviewer `rev_keyscan.py`：HEAD `38e20fa` 全部 **805** 個追蹤檔（含全部 worklog、audit、acceptance、PNG／DB 位元組）strict `KEY_PATTERN` **0**、寬鬆大小寫不敏感版 **0**；`git log -p --binary 08e158e..38e20fa`（37.9 MB）strict **0**、寬鬆 **0**；`df78e79..9902026` 同為 0。非空 `"Authorization": "…"` 的命中只有文件／佔位字串（老師原文 `YOUR_API_KEY`、`...`、掃描器說明、`test_secrets.py` 的假金鑰），無真值。 |
| **AC-V2-17(b)** 只從 `CWA_API_KEY` 讀；本機 `.env` 執行成功且終端無金鑰 | **PASS（依參照）** | 程式審查由 a35-r1、a40-r1 完成；本機 `.env` 實跑紀錄 wl35 V-13、wl36 V-13、wl40 V-7、wl41 V-9。Reviewer 依 RB-3／A-3 **未**以真金鑰重跑（不讀 `.env`）；README l. 294–298、421–430 的敘述與 `server.py` `observation.load_local_env(Path(__file__).resolve().parent / ".env")` 一致。 |
| **AC-V2-17(d)** 無金鑰 → `key_not_configured`、預報正常 | **PASS**（本機＋平台） | 本機 `test_no_key_in_process_env_is_key_not_configured` 在 614 中通過；**preview（無金鑰）**：`/api/observations/latest` **503** `{"dataset":"O-A0001-001","error":"Latest Observation is unavailable: the server has no CWA API key configured.","reason":"key_not_configured"}`、`cache-control: no-store`；`/api/radar/latest` **503** `{"dataset":"O-A0058-006",…,"reason":"key_not_configured"}`；`/api/health` **200**。 |
| **AC-V2-17(e)** worklog／audit／acceptance／截圖無金鑰；A-3 使用有紀錄 | **PASS** | 見 (a) 的全追蹤檔與 patch 掃描（涵蓋 `doc/governance/**`、`doc/acceptance/**`、全部截圖）；Reviewer 目視 ev41 五張最終截圖：只有合成／樣本資料，無金鑰（瀏覽器檢查本來就用 placeholder／哨兵金鑰且金鑰不到瀏覽器）。wl41 A-3 紀錄 1 列（本機 README 實跑，定向驗證，GET 2 次）。 |
| **AC-V2-17(c)** acceptor 填入後 preview 觀測／雷達 200、紀錄無金鑰 | **BLOCKED（RB-3）——不是 FAIL** | 見 §3。 |
| **AC-V2-22** 非金鑰部分（不需登入、`GET /` V2、`/api/health` smoke ≤ 90 s、部署 id ↔ commit） | **PASS** | `gh api repos/…/deployments?sha=9902026cc41725524f154185aeebd59057fbdadf` → deployment **`6677133574`**（environment Preview、ref／sha `9902026cc4…`、creator `vercel[bot]`、2026-09-26T09:25:30Z）；statuses → `success`、environment_url **`https://aiot-hw01-weather-cckt159kq-nchu-aiot-class.vercel.app`**。Reviewer 無 cookie、無登入：`python smoke.py <該 URL>` → `[2026-09-26T09:38:00Z] … GET / -> 200  GET /api/health -> 200  (6.0s elapsed)  PASS`、`SMOKE PASS`、exit 0；`GET /` 200 `text/html`，`<title>Taiwan Weather Forecast</title>`、`id="mode-now"`、`id="mode-forecast"`、`id="radar-toggle"`、`data-deployment-id="dpl_HVDtY3JaXwXWGCzBwjepGURRJntw"`；`/api/health` 200 `{"forecast_day_count":7,"ingestion_time":"2026-09-24T02:24:50+08:00","region_count":6,"status":"ok"}`。**內容對應**：服務的 `/static/app.js` sha256 `37d77e24bc0b…` ＝ `git show 9902026:home_work_01/static/app.js`；服務的 `index.html` 去掉 `data-deployment-id` 屬性後與 `9902026` blob 只差 Vercel 在 preview 注入的一行 toolbar `<script … src="https://vercel.live/_next-live/feedback/feedback.js">`（平台注入，V1 #21 R1 O-3／#25 R1 O-3 先例；見 F-2）。分支 alias 無法取得（Vercel API 403）改用每 commit deployment URL——受審 commit 的部署可達，接受。 |
| **AC-V2-22** 觀測部分；production | **BLOCKED（RB-3）**；production 為合併後 release evidence | 見 §3。 |
| **AC-V2-03** preview 抽樣 | **BLOCKED（RB-3）** | 見 §3。離線、API、瀏覽器部分由 #35／#36 結案（a35-r2、a36-r1），不在本票重判。 |
| **§6.3 定向 V1 重驗**（彙整） | **PASS** | `ACCEPTANCE-V2.md` §3 逐列涵蓋 Spec §6.3 全部列（AC-17／18、AC-19、AC-02／03／24、AC-04、AC-07(b)(c)(d)(f)、AC-07(e)、AC-10、AC-14、AC-15／16／22(a)、AC-26／INV-9、標題與 masthead、其餘 V1 AC）。Reviewer 抽核：**AC-14** 八項行號（l. 55–58、59–61、62–67、68–77、79–84、867–874、266–278、3–6／857–860）逐一落在對應文字，且 V2 新增的 F-D0047-091 授權列（l. 885）明寫「project-derived」；**AC-07(e)** supersede 記述在 `ACCEPTANCE-V2.md` §3 與 V1 `ACCEPTANCE.md` 新增段；**AC-10** Reviewer 在 `9902026` 匯出樹重跑 `check_series_error_visible.py` → `PASS`（503、404 皆有可見訊息）；**AC-15／16／22(a)** 本表 AC-V2-22 與 AC-V2-20 列；**AC-26／INV-9** `app.py`／`requirements.txt` blob ＝ V1；**AC-17／18** `check_modes_browser.py` 三次 37/37。其餘列引用的 ticket 證據與數字（例如 30.2 %／46.9 %、28.95 px/km、12.96 km、52.5 %、0.0075／0.067／0.72／3.83 km、8.4 s／20.4 s、876／849）Reviewer 以 grep 對照 wl37、wl39、wl40、wl35 原文一致；#35～#40 audit record 最後一行 verdict：a35-r2、a36-r1、a37-r1、a38-r2、a39-r2、a40-r1 皆 `VERDICT: CLOSURE`。 |
| **R-V2-DOC-3** V2 驗收文件 | **PASS** | `doc/acceptance/ACCEPTANCE-V2.md` 存在；§1 有 AC-V2-01～23 共 23 列，各有狀態、owner、驗證方式與證據引用；§3 為 §6.3；V1 `ACCEPTANCE.md` 在 `08e158e..9902026` 的 diff **只有**開頭新增 6 行參照 blockquote，既有條目 0 行被改寫。 |
| **Bindings §5 release-gate 材料**（本票分配部分） | **PASS**（A-6 preview 紀錄 BLOCKED） | README 實跑：Reviewer 抽核 V-9 的離線 rebuild——在 `9902026` 匯出樹執行 `python -m ingestion --from-json data/raw/F-D0047-091.json --db <scratch>` → exit 0、`Persisted 42 rows … (ingested at 2026-09-24T02:24:50+08:00)`；與已提交 `data.db` 的 42 列、`IngestionMetadata`、`sqlite_master` 全部相同。`git ls-files` 無 `.env`；憑證掃描 0；老師 SQL（下列 H-2）；最後一次 CI 綠（`36232697465`，記於 wl41 V-2；`ACCEPTANCE-V2.md` §7 以引用指向）。SIA 與 DA phase acceptance 在本票之後，不屬本票。 |

### 2.1 AC-V2-21 十四項（README `9902026` 行號，Reviewer 自行閱讀判定）

| # | 項目 | 判定 | Reviewer 的依據 |
| --- | --- | --- | --- |
| 1 | Now／Forecast 說明；Forecast mode 有專屬標題、目錄直達 | PASS | Contents l. 21–47，l. 39 為**粗體獨立項**直達 l. 822 `### Forecast mode — the Part A bonus map: six-region Taiwan Map and \`Select Date\``（slug 可解析）；兩模式對照表 l. 506–518；scope note l. 12–16 亦連結。不是「段落中間一句帶過」。 |
| 2 | O-A0001-001 來源與逐時節奏（保守） | PASS | l. 342–344「CWA describes it as hourly station data」、l. 546–551「When CWA publishes a new hour is up to CWA」。 |
| 3 | 代表測站規則可手算 | PASS | l. 784–820 四步驟＋例子；Reviewer 依 README 文字自寫選取（偏好表依 l. 803–808 所指的 `representative.PREFERRED_STATION`），對樣本經未修改的 `LatestObservationService` 產生的 `/api/` 本文：**22／22 縣與伺服器 `representativeStationIds` 相同**（臺北市 466920、高雄市 467441、金門縣 467110、連江縣 467990、澎湖縣 467350）；後備：移除偏好站後 臺北市→`466910`、嘉義縣→`467530`、花蓮縣→`72T250`，與 `select_representatives` 相同。README 未列舉 22 個 StationId（DD-3(f)）。 |
| 4 | 圍欄外測站規則 | PASS | l. 646–652（範圍、東沙島 `468100` 116.73E、計數／清單「not on the map」／可看詳情）。 |
| 5 | 觀測（如發布）與推導值分開；無 DOC-5 (a)–(d) 反例 | PASS | l. 86–103 對照表（`OBSERVED` vs `DERIVED`，兩種時間標籤）、l. 520–530、l. 601–607、l. 620–630（無縣平均）、l. 866–874。全文 `real-time`／`realtime`／`live` 只剩 l. 1120 的動詞「live in」（非指觀測）。F-D0047-091 授權列明寫值為 project-derived。`Fetched Time` 與 `Last updated (data fetched from CWA)` 於 l. 98、l. 526–530 明確分開。 |
| 6 | CWA 授權標示兩個資料集 | PASS | l. 876–891：**交通部中央氣象署 氣象觀測站-全測站逐時氣象資料 (O-A0001-001)**、**交通部中央氣象署 雷達整合回波圖-臺灣(鄰近地區)_透明底圖 (O-A0058-006)**，授權 Open Government Data License（政府資料開放授權條款）；另 l. 780–782。應用內 SHOULD 見 §4(a)。 |
| 7 | Vercel 金鑰步驟（名稱、環境、acceptor 填入、**不含值**）與本機 `.env` | PASS | l. 913–933：`CWA_API_KEY`、Production＋Preview、「Only the repository owner does this … an agent never enters, reads, prints or exports the key (reserved boundary RB-3)」、redeploy、不讀值的檢查、禁止 `vercel env pull`；l. 903–911 何者需要金鑰；`.env`：l. 130–151、294–298、421–430。無任何值（金鑰格式掃描 0）。與 Spec §9 前置條件 1「preview 與 production」一致。 |
| 8 | 觀測／雷達 endpoint、失敗代碼、逾時、重用視窗 | PASS | 觀測 l. 340–437（欄位、四個 `reason` 與 HTTP 碼 l. 393–405、3 s／5 s／8 s 與頁面 20 s、重用 300 s l. 407–419）；雷達 l. 439–502（標頭、產品檢查、四個 `reason` l. 471–483、8 s、重用 120 s l. 485–494）。數值與程式一致：`observation.py:134–141`（3.0／5.0／8.0／300／600）、`radar.py:98–104`（3.0／5.0／8.0／120／300）、`app.js:263`、`app.js:297`（20000 ms）。 |
| 9 | 圍欄／縮放 | PASS | l. 663–682（lat 21.2–26.7、lon 117.6–122.9、zoom 6–12、初始視野）；與 `app.js:223`（`MAP_RANGE`）、`app.js:233–234`（`MIN_ZOOM` 6、`MAX_ZOOM` 12）一致；l. 699–700 的「1280 px 9 個、手機 5 個代表標記」與 ev41 兩張最終截圖目數相符。 |
| 10 | Radar 來源、對齊方式、重新取得觸發、已知限制 | PASS | l. 439–446、720–782：來源 O-A0058-006、圖層順序、觸發（開啟／顯示中 Refresh，不輪詢）l. 739–745、24 條帶對齊 l. 756–770、限制（產品延遲、metadata 與影像週期可能不一致——R-V2-RAD-4 要求的已知限制）l. 771–779。 |
| 11 | Later 清單 | PASS | l. 1152–1172：Spec §8 OPTIONAL／Later 十三項全部在列（雨量圖層、雷達動畫／回放、heatmap、自動更新／輪詢、縣市篩選與測站搜尋、URL deep-linking、瀏覽器歷史、10 分鐘資料集、歷史觀測儲存、Windy、22 縣市預報、Grading App V2 行為、速率限制）。 |
| 12 | 與 V2 矛盾的舊敘述已修正；V1 預報路徑不需 secret 保留 | PASS | Reviewer 檢視 `df78e79..49dac12` README 的全部刪除行：「read solely by the ingestion fetch stage」、「The running function needs no environment variable and no secret — the CWA key is never part of the deployment」、「Every Refresh — and the page's first load — ends in…」、V1 分支 alias URL 與其 smoke 範例、只列 V1 fixture 的憑證掃描敘述、poster 對照表列——全部改為 V2 事實（l. 141–151、903–911、559–562、945–959、971、1130–1135、1190）。保留：l. 903–905「needs **no environment variable and no secret**, exactly as in V1」（預報部分）、l. 336–338。 |
| 13 | `CONTEXT.md` 詞彙 delta 逐字 | PASS | BRIEF-V2 §9 十列與 `CONTEXT.md` l. 97–99、122–132、146–170 逐字相同（Reviewer 目視對照）；`test_context_glossary_delta_is_verbatim` **自 BRIEF 原始檔解析**十列再比對（不是複本），並斷言舊 Refresh 意義已移除，在 614 中通過。`df78e79..9902026` 未改 `CONTEXT.md`。 |
| 14 | V2 驗收文件逐條對應 AC-V2 與 §6.3；V1 `ACCEPTANCE.md` 未改寫 | PASS | 見 R-V2-DOC-3 列。`ACCEPTANCE-V2.md` §2 的 README 行號引用 Reviewer 抽核全部落在所述文字。數字誤記見 F-1（不影響對應完整性）。 |

## 3. RB-3 金鑰閘門（BLOCKED，不是 FAIL）

- **狀態確認**：final subject 的 preview（deployment `6677133574`）上 `/api/observations/latest` 與 `/api/radar/latest` 皆回 **503 `key_not_configured`**、`/api/health` 200——**證實 Vercel 金鑰尚未填入**，且缺金鑰時的平台行為正確（AC-V2-17(d)、INV-V2-4）。這是治理 §1.5 第 3 項（Bindings §2.6 RB-3 保留給 acceptor）的真正 boundary，Spec §9 前置條件 1、derivation §11 #4 與本 Issue 皆明定在此之前記 BLOCKED（不是 FAIL）。
- **受影響項目正確記為 BLOCKED**：`ACCEPTANCE-V2.md` §1（AC-V2-03、17、22 列）、§5（AB-V2-2、10、13）、§6.2、§7（A-6）與 wl41 Remaining work 1 一致，**沒有任何一項被標成 PASS**。
- **驗證計畫（§6.2 P-1～P-4）**：前提（acceptor 依 README 步驟 1–3 填入 Production＋Preview 並 redeploy）、每項的驗證動作與 PASS 條件都可客觀執行：P-1 覆蓋 AC-V2-17(c) 觀測與 AC-V2-22 觀測部分（200、`dataset`、`validStationCount` ≥ 1、`stations[]`、兩個時間；瀏覽器 Now mode 顯示 Latest Observation；smoke 仍 PASS；deployment ↔ commit）；P-2 覆蓋雷達 200 `image/png`＋`X-Radar-Time`＋`X-Radar-Dataset` 與平台面 INV-V2-2；P-3 覆蓋 AC-V2-03 preview 抽樣（≥ 3 站、頁面值 ＝ 同頁 `/api/` 本文、哨兵「—」）；P-4 覆蓋 A-6 紀錄與 Vercel 時序（冷啟動、並行、8 s 上限 vs 平台時限，低於假設時 route DA——derivation §11 #1）。兩處程序補充見 F-2。
- **注意（routing）**：AC-V2-17(c) 與 AC-V2-22 的證據類別是「preview 驗證紀錄（**Reviewer 重現**）」。金鑰填入後，P-1～P-3 的紀錄須由獨立 Reviewer 重現（可在 SIA 內，若屆時金鑰已填入；否則由 Orchestrator 另行安排 post-key 驗證），不能只以 Executor 紀錄結案。本 R1 的 CLOSURE **不**把任何 RB-3 項目記為 PASS。

## 4. Executor concerns 的判定

- **(a) 應用內觀測授權標示（R-V2-DOC-2 SHOULD、DV-16）**：`static/index.html` l. 283「CWA station observations (O-A0001-001, hourly), shown as published.」只以縮寫與資料集 id 標示，無「交通部中央氣象署 氣象觀測站-全測站逐時氣象資料」全名；雷達列 l. 291–292 為全名。判定：**不是 DOC-2 finding**——DOC-2 的 MUST 只在 README（兩個資料集全名皆具，§2.1 #6）；應用內為 SHOULD（B-14：「可偏離並記理由，不是新功能」），且目前是**部分滿足**（標示了 CWA 與資料集）。偏離理由已記於 wl41 決定 5 與 `ACCEPTANCE-V2.md` §8（Spec §0「SHOULD 偏離須在 worklog 記理由」成立）；補一行文字屬 UI 變更，超出本票「不做任何行為變更」的範圍。**SHOULD-acceptable，non-blocking**；若 acceptor 或 SIA 想補齊，是一行 `index.html` 文字的後續項（owner：acceptor 指示或 SIA 的整合觀點）。
- **(b) `check_modes_browser.py` 去 flake（#38 R2 N-1）**：`df78e79..49dac12` 的 diff 只新增 `SecondApartClock` 類別並以 `clock=SecondApartClock()` 注入測試台的 `LatestObservationService`；l. 797–798 的斷言（等待 `obs-time` 以 `NEXT_HOUR` 日期開頭）與其餘檢查**逐字未變**。Reviewer 在 `9902026` 匯出樹連跑三次 **37/37**。**Mutation test**（scratch 副本，把時鐘改為凍結常數＝強制重現「同一秒」條件）→ **34/37**：`B-desktop`／`B-375 AC-V2-09(a) Refresh works while the forecast snapshot is unavailable` 與 `A-desktop OBS-7 Refresh success…` FAIL（頁面正確顯示「Already the latest」）——斷言仍具鑑別力。去 flake 只消除測試台的隨機條件；「同一 Fetched Time → not-newer」的產品路徑由 #37 的 refresh 檢查涵蓋（a37-r1）。**未弱化**。
- **(c) 第一個 commit 使用 `git -c core.hooksPath=`**：`git config --show-origin` 在 local／global／system 皆無 `core.hooksPath`；`.git/hooks/` 只有 `*.sample`；root 無 pre-commit 設定。**沒有任何 hook 被略過——無害**，不列 finding。
- **(d) #38～#40 地圖可用性交接（`ACCEPTANCE-V2.md` §8）**：Reviewer 對照各結案 audit：a38-r1 F-2（l. 106 交 #39＋SIA）、a38-r2（l. 69 仍交 SIA）、a39-r1 F-3／O-2、a39-r2（l. 65、l. 77 O-6）、a40-r1 F-1（l. 91 Owner：SIA）——§8 皆已列出且 owner 為 SIA；a38-r2 N-1 的 owner（#41）已在本票處理。**依指示留給 SIA，不在此重判。**

## 5. 高風險類別核對段（decision A-1；derivation §6）

本票觸及 **H-1、H-2、H-3**。

- **H-1 憑證與機密——核對了什麼**：(a) README 金鑰步驟只有變數名、環境、流程與不讀值的檢查，**無值**；明文 agent 不得填入／讀出／印出／匯出（RB-3）。(b) `git ls-files` 無 `.env`（只有 `.env.example`）；`.env` 被 `home_work_01/.gitignore:12` 與 root `.gitignore:11` 忽略。(c) 全部 805 個追蹤檔（含 worklog、audit、acceptance、截圖位元組）與 `08e158e..38e20fa` 全部 patch 的金鑰格式掃描 0；CI 掃描 804 檔通過。(d) preview 紀錄（`ACCEPTANCE-V2.md` §6.1、wl41 V-11）只含 `key_not_configured` 本文；Reviewer 取得的 preview 回應亦只有非機密的固定句。(e) b3 兩個授權位置：本機 `.env`（未追蹤）；Vercel（尚未填入，§3 證實）。(f) Reviewer 自身未讀 `.env`、未接觸 Vercel 值。**結果：成立。**
- **H-2 老師指定的介面或資料格式——核對了什麼**：(a) `app.py`、`weather_query.py`、`ingestion/`、`data.db`、`requirements.txt`、`smoke.py`、`vercel.json`、`data/raw/`、`.python-version`、workflows 與 V1 `ef15d3e` blob／tree 相同（§2 AC-V2-20 列）。(b) Reviewer 以唯讀開啟 `git show 9902026:home_work_01/data.db`（sha256 `9bbf05bc6cc803444c8760432d6b484699c597f751fa16cb58bfbb5a0dbf542b`，＝工作樹）執行老師兩句 SQL：`SELECT DISTINCT regionName FROM TemperatureForecasts;` → **6 列**（北部地區、中部地區、南部地區、東北部地區、東部地區、東南部地區）；`SELECT * FROM TemperatureForecasts WHERE regionName = '中部地區';` → **7 列**（id 8–14，2026-09-24…2026-09-30）；共 42 列、(regionName, dataDate) 重複 0；DDL 逐字 `id INTEGER PRIMARY KEY, regionName TEXT, dataDate TEXT, mint REAL, maxt REAL`。(c) 預報 `/api/` 126 組與 V1 byte 相同（§2）。(d) preview `<title>Taiwan Weather Forecast</title>`；概念詞由 `test_verbatim_labels` 等在 614 中守住；截圖 `<h1>` 逐字。(e) README 對照表 l. 1174–1191 保留老師檔名。**結果：成立。**
- **H-3 資料語義與標示——核對了什麼**：(a) README：觀測＝CWA 測站觀測（如發布，`OBSERVED`）、預報＝專案推導（`DERIVED`／`PROJECT-DERIVED COMPATIBILITY VALUES`），不共用圖例／色階（l. 86–103、520–525）；(b) 代表測站值永不當縣值、無縣平均或聚合（l. 89–91、601–607、620–630）；(c) 無 `real-time`／`realtime`／`live` 指稱觀測（README、`ACCEPTANCE-V2.md`、`index.html` grep）；(d) `Fetched Time` 與預報快照取得時間分開（l. 98、526–530）；(e) 授權標示：README 兩個 V2 資料集全名＋F-D0047-091 註明 project-derived；應用內觀測列為 SHOULD 部分滿足（§4(a)）；(f) 頁面：Reviewer 目視 ev41 五張最終截圖——Now mode「Latest Observation／OBSERVED」、「CWA station observations (O-A0001-001, hourly), shown as published.」、「Each marker is one representative station's air temperature (°C) — a station value, not a county temperature.」；Forecast mode「DERIVED」、「Source: CWA F-D0047-091 (county-level) → project-derived six-region values」、「derived, not an observed daily mean」；375 px 預報區段「Last updated (data fetched from CWA)」與 Now 面板 `Fetched Time` 分處不同卡片。V1 AC-14 八項未變（§2 §6.3 列）。**結果：成立。**

## 6. Findings

### F-1（Low，non-blocking）——`ACCEPTANCE-V2.md` 與 wl41 的最終 network-log 數字誤記

- **證據**：`ACCEPTANCE-V2.md` l. 113（AC-V2-16 列）「all six final logs together: 50 + 203 + 136 + 506 + 113 requests」、l. 168（INV-V2-3 列）「1,008 requests across the six logs」；wl41 V-7a 同為 136／506／1,008。已提交的 ev41 只有 **五個** network log；其內容與各自的結果檔記錄為 county **135**（`final-check-county-results.json` l. 1264 `"requests": 135`）、fence **505**（`final-check-fence-results.json` l. 8197 `"requests": 505`），合計 **1,006**。Reviewer 自行解析五個檔案的 URL：50＋203＋135＋505＋113＝1,006，外部 0。
- **契約條款**：R-V2-DOC-3（驗收文件記錄證據引用）；AC-V2-16 的結論（零外部）**不受影響**。
- **Disposition**：editorial，不延長本 cycle。Owner：下一次編輯 `ACCEPTANCE-V2.md` 的角色（例如記錄 P-1～P-4 結果時）順修為「five logs … 135 … 505 … 1,006」。

### F-2（Low，non-blocking）——§6.2 post-key 驗證計畫缺兩項程序註記

- **證據**：(a) Reviewer 取得的 final-subject preview `index.html` 含 Vercel 在 preview 注入的 `<script async data-explicit-opt-in="true" src="https://vercel.live/_next-live/feedback/feedback.js">`（不在 subject 內；production 不注入；V1 #21 R1 O-3、#25 R1 O-3 已記為平台注入）。P-1「open `<preview>/` in a browser」若同時錄 network log，會看到這個外部請求；`ACCEPTANCE-V2.md` §6 未提醒應歸為平台注入、而非 AC-V2-16／INV-V2-3 的前端違規（AC-V2-16 的證據本來就是 loopback 上的 log）。(b) P-2「scan … for the key」未寫明方法；依 RB-3，驗證者不得讀出 Vercel 值——應寫明以金鑰格式（`ingestion/checks.py` `KEY_PATTERN`／`tools.credential_scan` 同一規則）掃描；若 acceptor 確認 Vercel 與本機 `.env` 為同一把金鑰，另可在 A-3 範圍內以只輸出布林的字面比對補強。
- **契約條款**：RB-3（b3）；INV-V2-2、INV-V2-3；AC-V2-17(c)、AC-V2-22 的「Reviewer 重現」證據類別。計畫其餘部分精確、可執行（§3），故不 blocking。
- **Disposition**：Owner：**Orchestrator**——在 post-key 驗證的派工內容中附上這兩項註記（或由記錄 P-1～P-4 結果的角色在 `ACCEPTANCE-V2.md` §6.2 補一句）。不延長本 cycle。

（無 Critical／High／Medium finding。）

## 7. 觀察（非 finding；供 SIA 參考）

- **O-1**：`CONTEXT.md`「Web App」詞條仍寫「read the persisted Forecast Snapshot (never call CWA)」。以 INV-V2-1（預報路徑 CWA-free）解讀時與 V2 不矛盾；R-V2-DOC-4 只要求 BRIEF-V2 §9 的 delta 逐字併入，改其他詞條不在本票範圍（wl41 決定 6 同判）。SIA 可在整合觀點下評估讀者是否會把它讀成「Dashboard 伺服器不碰 CWA」。
- **O-2**：a40-r1 O-3（雷達影像請求跟隨重新導向；「記錄供 SIA 知悉」）未列入 `ACCEPTANCE-V2.md` §8。它不是帶 disposition 的交接項，SIA 會直接讀 audit records；列此只為完整。
- **O-3**：decision A-6 要求「A-5 的最後一次 CI 結果引用記在 `doc/acceptance/`」。final subject 的 CI run id 必然產生在該 commit 之後，因此只記在 wl41 V-2，`ACCEPTANCE-V2.md` §7 以引用指向 wl41——Reviewer 認為合理。release 時 acceptor 應引用實際合併 commit 的 CI。
- **O-4**：README l. 1017「The suite is fully offline: it never calls the network」——套件中有 loopback 伺服器（停滯上游模擬），「network」指外部網路；V1 既有措辭，不影響 AC-V2-21。

## 8. Routing

| 事項 | Authority | 理由 |
| --- | --- | --- |
| 在 Vercel 專案填入 `CWA_API_KEY`（Production＋Preview）並 redeploy，解除 AC-V2-17(c)、AC-V2-22 觀測部分、AC-V2-03 preview 抽樣、A-6 preview 紀錄的 BLOCKED | **acceptor**（RB-3，Bindings §2.6） | 保留 boundary；Agents 不得填入／讀出。 |
| 金鑰填入後由獨立 Reviewer 重現 P-1～P-3（附 F-2 兩項註記） | **Orchestrator**（派工）；判定由 Reviewer | AC-V2-17(c)、AC-V2-22 的證據類別要求「Reviewer 重現」。 |
| P-4 若平台時限低於 8 s 上游上限的假設 | **Design Authority** | derivation §11 #1（已記於 `ACCEPTANCE-V2.md` §6.2）。 |
| #41 在 RB-3 項目 BLOCKED 下的結案狀態記述 | **Orchestrator**（依治理 §1.5、§3.8 陳述；不得把 BLOCKED 記成 PASS） | 本 R1 只判定 findings／closure。 |
| 應用內觀測授權全名（選擇性補一行 UI 文字） | **acceptor**（若要）或 SIA 整合觀點 | SHOULD 已記理由；超出本票範圍。 |
| 合併（RB-1）、繳交（RB-2） | **acceptor** | 保留 boundary；production smoke 與觀測抽樣為合併後 release evidence。 |

未發現需要 Design Authority 裁決的契約語義不足或 boundary 疑義。

## 9. 結論

本票可完成的範圍（AC-V2-21 十四項、AC-V2-20、AC-V2-16、AC-V2-17(a)(b)(d)(e)、AC-V2-22 非金鑰部分、§6.3 彙整、R-V2-DOC-3、release-gate 材料）經 Reviewer 自行取得的證據逐項 **PASS**；H-1／H-2／H-3 成立；無 blocking finding（F-1、F-2 皆 Low）。**AC-V2-17(c)、AC-V2-22 觀測部分、AC-V2-03 preview 抽樣與 A-6 preview 紀錄維持 BLOCKED（RB-3），本紀錄不將其記為 PASS**；解除後須依 §3／§8 由獨立 Reviewer 重現。

VERDICT: CLOSURE
