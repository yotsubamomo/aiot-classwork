# HW10 Taiwan Weather Forecast — pinned dependencies (Python 3.12).
# Ingestion (Issue #18): fetch + derive + persist + offline tests.
# Grading App (Issue #19): the Streamlit app and the shared query module.
# Dashboard (Issue #20): the Flask backend that serves the page and the /api/ JSON
# and deploys to Vercel as a single function. Its static frontend needs no build
# step and no extra runtime dependency (the chart is hand-drawn inline SVG).
# The map libraries used by the later ENHANCED dashboard work are intentionally not
# listed here; the MVM apps must not depend on them (Spec R-ENV-1, R-GA-9).
requests==2.32.3
pytest==8.3.3
streamlit==1.64.0
flask==3.1.2
